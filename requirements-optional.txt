# Optional Dependencies - Platform Specific
# Generated by scripts/generate_final_requirements.py
#
# These packages are INCLUDED in requirements.txt but MAY FAIL on some platforms
# If you encounter issues, install requirements-core.txt first, then these separately
#

# Heavy ML Frameworks (may require specific Python versions or CUDA)
tensorflow==2.18.0  # Requires Python 3.11+ for this version
torch==2.8.0  # Install with appropriate CUDA/CPU build from pytorch.org

# Complex Dependencies (may fail to build on some systems)
pymc==5.16.2  # Requires NumPy 1.x and specific compiler toolchain
pytensor==2.34.0
arviz==0.20.0
dowhy==0.12
econml==0.15.1

